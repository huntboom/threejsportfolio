{
  "name": "three-demo",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite serve dist",
    "build": "vite build",
    "deploy": "gh-pages -d dist",
    "serve": "vite preview"
  },
  "devDependencies": {
    "vite": "^2.3.0"
  },
  "dependencies": {
    "gh-pages": "^4.0.0",
    "three": "^0.128.0"
  }
}
